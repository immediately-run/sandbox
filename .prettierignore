fixture
fixtures

# Generated by scripts/protocol-codegen (R3-274b): the generator owns the bytes, and
# the codegen drift gate compares them. A prettier pass here would fight that gate —
# CI runs `format -- --check`, which is prettier `--write`, BEFORE the drift check.
src/generated/protocol.ts
generated/
