// GENERATED by sandbox scripts/protocol-codegen/generate.mjs — DO NOT EDIT.
// Source of truth: sandbox scripts/protocol-codegen/descriptors.protocol.mjs
// descriptorsHash: fbd9b76f9e8edc71
//
// The sandbox↔SDK wire vocabulary (PLATFORM_LAYERING_SPEC §2 / S1). Import the
// constants instead of writing the string: a rename then has ONE edit site and
// the protocol snapshot gate sees it. Payload types are emitted for every name
// that has a declared shape; a name handled as `any` today gets its read field
// list in the doc comment instead (R3-274e is where those grow real types).

/** Every wire name this side speaks, as a union. */
export type WireName =
  | 'action'
  | 'api-catalog'
  | 'artifact-distrust'
  | 'auth-state'
  | 'console'
  | 'done'
  | 'editor-context'
  | 'evaluate'
  | 'form-factor'
  | 'fs-change'
  | 'initialized'
  | 'ir-marker'
  | 'mount-add'
  | 'mount-remove'
  | 'refresh'
  | 'register-frame'
  | 'repo-mount'
  | 'request-api-catalog'
  | 'request-auth-state'
  | 'request-editor-context'
  | 'request-form-factor'
  | 'request-handshake'
  | 'request-mounts'
  | 'request-repo-mount'
  | 'request-theme'
  | 'request-vcs-state'
  | 'resize'
  | 'sdk-handshake'
  | 'start'
  | 'state'
  | 'status'
  | 'success'
  | 'theme'
  | 'vcs-state';

/** `action` — message · app->host. */
export const ACTION = 'action';
export interface ActionPayload {
  action: string;
  column: null | number;
  line: null | number;
  message: string;
  path: any;
  payload: {
    frames: Array<unknown /* StackFrame */>;
  };
  title: string;
}

/** `api-catalog` — push · host->app. polled with `request-api-catalog`. */
export const API_CATALOG = 'api-catalog';
export interface ApiCatalogPayload {
  methods: Array<{
    capability: string;
    name: string;
    stream?: boolean | undefined;
  }>;
}

/** `artifact-distrust` — message · app->host. */
export const ARTIFACT_DISTRUST = 'artifact-distrust';
export interface ArtifactDistrustPayload {
  commitSha: null | string;
  reason: string;
}

/** `auth-state` — push · host->app. polled with `request-auth-state`. */
export const AUTH_STATE = 'auth-state';
/** Read off this message today: `state`. */

/** `console` — message · app->host. */
export const CONSOLE = 'console';
export interface ConsolePayload {
  log: any;
}

/** `done` — message · app->host. */
export const DONE = 'done';
export interface DonePayload {
  compilatonError: boolean;
}

/** `editor-context` — push · host->app. polled with `request-editor-context`. DIVERGENT (R3-274e): declared-incomplete — the host (site-main channelBridge.ts) sends {dirtyPaths, openFiles, activeFile, viewedFile}; this side declares only {dirtyPaths, activeFile?} while the SDK reads all four. `viewedFile` (R3-268) and `openFiles` never entered this declaration. R3-274e picks one shape for both sides.. */
export const EDITOR_CONTEXT = 'editor-context';
export interface EditorContextPayload {
  activeFile?: null | string | undefined;
  dirtyPaths: Array<string>;
}

/** `evaluate` — message · host->app. */
export const EVALUATE = 'evaluate';
/** Read off this message today: `command`. */

/** `form-factor` — push · host->app. polled with `request-form-factor`. */
export const FORM_FACTOR = 'form-factor';
/** Read off this message today: `formFactor`. */

/** `fs-change` — message · host->app. DIVERGENT (R3-274e): subset-reads with no declared owner on either side — the host sends {paths, epoch}; this side reads only `paths`, the SDK reads both. Nothing forces the two to agree, which is the divergence PLATFORM_LAYERING_SPEC §2 names as the exemplar. The `kind` also differs (message here, push channel in the SDK). R3-274e decides: keep `epoch` or drop it.. */
export const FS_CHANGE = 'fs-change';
/** Read off this message today: `paths`. */

/** `initialized` — message · app->host. */
export const INITIALIZED = 'initialized';

/** `ir-marker` — message · app->host. */
export const IR_MARKER = 'ir-marker';
export interface IrMarkerPayload {
  at: number;
  attrs?: Record<string, unknown> | undefined;
  name: string;
}

/** `mount-add` — message · host->app. */
export const MOUNT_ADD = 'mount-add';
/** Read off this message today: `mount`, `ports`. */

/** `mount-remove` — message · host->app. */
export const MOUNT_REMOVE = 'mount-remove';
/** Read off this message today: `id`, `path`, `reason`. */

/** `refresh` — message · both. */
export const REFRESH = 'refresh';

/** `register-frame` — message · host->app. */
export const REGISTER_FRAME = 'register-frame';
/** Read off this message today: `dirtyPaths`, `distrustArtifacts`, `fsSnapshot`, `id`, `logLevel`, `region`, `sdkIntegrity`, `template`. */

/** `repo-mount` — push · host->app. polled with `request-repo-mount`. */
export const REPO_MOUNT = 'repo-mount';
/** Read off this message today: `path`, `uri`. */

/** `request-api-catalog` — poll · app->host. */
export const REQUEST_API_CATALOG = 'request-api-catalog';

/** `request-auth-state` — poll · app->host. */
export const REQUEST_AUTH_STATE = 'request-auth-state';

/** `request-editor-context` — poll · app->host. */
export const REQUEST_EDITOR_CONTEXT = 'request-editor-context';

/** `request-form-factor` — poll · app->host. */
export const REQUEST_FORM_FACTOR = 'request-form-factor';

/** `request-handshake` — message · host->app. */
export const REQUEST_HANDSHAKE = 'request-handshake';

/** `request-mounts` — message · app->host. */
export const REQUEST_MOUNTS = 'request-mounts';

/** `request-repo-mount` — poll · app->host. */
export const REQUEST_REPO_MOUNT = 'request-repo-mount';

/** `request-theme` — poll · app->host. */
export const REQUEST_THEME = 'request-theme';

/** `request-vcs-state` — poll · app->host. */
export const REQUEST_VCS_STATE = 'request-vcs-state';

/** `resize` — message · app->host. */
export const RESIZE = 'resize';
export interface ResizePayload {
  height: number;
}

/** `sdk-handshake` — message · app->host. DIVERGENT (R3-274e): divergent-declared — TWO producers of one name with different payloads: this frame announces {protocolVersion, sandboxProtocolVersion}, the SDK announces {protocolVersion, sdkVersion}. R3-274d gave the frame its own SANDBOX_PROTOCOL_VERSION (announced additively; the host logs it, fail-open) and retired the sibling-checkout read that used to source `protocolVersion`. R3-274e picks one shape.. */
export const SDK_HANDSHAKE = 'sdk-handshake';
export interface SdkHandshakePayload {
  protocolVersion: string;
  sandboxProtocolVersion: string;
}

/** `start` — message · app->host. */
export const START = 'start';
export interface StartPayload {
  firstLoad: boolean;
}

/** `state` — message · app->host. */
export const STATE = 'state';
export interface StatePayload {
  state: {
    transpiledModules: unknown /* { [x: string]: { source: { isEntry: boolean; fileName: string; compiledCode: string | null; }; }; } */;
  };
}

/** `status` — message · app->host. */
export const STATUS = 'status';
export interface StatusPayload {
  status: "evaluating" | "idle" | "initializing" | "installing-dependencies" | "running-tests" | "transpiling";
}

/** `success` — message · app->host. */
export const SUCCESS = 'success';

/** `theme` — push · host->app. polled with `request-theme`. */
export const THEME = 'theme';
export interface ThemePayload {
  theme: "dark" | "light";
}

/** `vcs-state` — push · host->app. polled with `request-vcs-state`. */
export const VCS_STATE = 'vcs-state';
export interface VcsStatePayload {
  branch?: {
    name: string;
    parentCommitSha: string;
    parentRef: string;
    parentRepo: string;
    upstreamPushable: boolean | null;
  } | null | undefined;
  changes: Array<{
    path: string;
    status: "created" | "modified" | "deleted";
  }>;
  diffLoading?: false | true | undefined;
  prs?: Array<unknown /* VcsPR */> | undefined;
}

/** Every wire name this side speaks, keyed by its constant. */
export const WIRE_NAMES = {
  ACTION,
  API_CATALOG,
  ARTIFACT_DISTRUST,
  AUTH_STATE,
  CONSOLE,
  DONE,
  EDITOR_CONTEXT,
  EVALUATE,
  FORM_FACTOR,
  FS_CHANGE,
  INITIALIZED,
  IR_MARKER,
  MOUNT_ADD,
  MOUNT_REMOVE,
  REFRESH,
  REGISTER_FRAME,
  REPO_MOUNT,
  REQUEST_API_CATALOG,
  REQUEST_AUTH_STATE,
  REQUEST_EDITOR_CONTEXT,
  REQUEST_FORM_FACTOR,
  REQUEST_HANDSHAKE,
  REQUEST_MOUNTS,
  REQUEST_REPO_MOUNT,
  REQUEST_THEME,
  REQUEST_VCS_STATE,
  RESIZE,
  SDK_HANDSHAKE,
  START,
  STATE,
  STATUS,
  SUCCESS,
  THEME,
  VCS_STATE,
} as const;
